# هسته سیستم — نسخه‌ها پین شده‌اند تا روی سیستم کاربر همیشه یکسان کار کند
requests>=2.31,<3
# رابط وب
fastapi>=0.110,<0.116
uvicorn>=0.29,<0.35
httpx>=0.27,<0.29
# کپی متن پیام در کلیپ‌بورد (اختیاری، برای فرمان draft)
pyperclip>=1.8 ; sys_platform != "linux"

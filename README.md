# 🏷️ دیوار لید — سیستم تجاری جمع‌آوری سرنخ از دیوار

رابط گرافیکی فارسی (وب محلی) + موتور جمع‌آوری چند‌اکانته + ضد بلاک +
مانیتور لحظه‌ای + دیتابیس سرنخ + خروجی اکسل.

## 🚀 اجرا (روی سیستم داخل ایران)
```bash
pip install -r requirements.txt
python -m marketing_divar.web        # مرورگر باز می‌شود: http://localhost:8642
```
یا ویندوز: دابل‌کلیک روی `scripts/run_web.bat`

## 🖥️ استفاده (۷ تب)
1. **داشبورد** — شروع/توقف اسکن + تیک «موارد فعلی هم گرفته شوند؟» + رخدادهای زنده
2. **اکانت‌ها** — لاگین با شماره موبایل + کد پیامکی؛ چند اکانت؛ آزادسازی بعد از کپچا
3. **کلمات کلیدی** — چند کلمه با کاما + انتخاب شهر (یا کل کشور)
4. **پیام‌ها** — قالب پیام چت (شخصی‌سازی با `{title}`) و پیامک (برای آینده)
5. **سرنخ‌ها** — لیست شماره‌دار/فقط‌چت + خروجی اکسل
6. **تنظیمات** — اتصال تلگرام (BotFather)، سهمیه‌ها و تاخیرهای ضد بلاک، سرویس پیامک (آینده)
7. **لاگ‌ها** — گزارش زنده + فایل `logs/divar_app.log` (چرخشی)

## 🛡️ ضد بلاک (به‌صورت خودکار فعال)
| لایه | مقدار پیش‌فرض |
|---|---|
| فاصله شماره‌گیری | ≥ ۱۰ ثانیه + جیتر |
| سقف هر اکانت / کل IP در روز | ۶۰ / ۲۴۰ |
| کپچای یک اکانت | بقیه بدون توقف ادامه می‌دهند + اعلان تلگرام |
| حالت‌ها | captcha / cooldown / relogin / disabled |

## 📦 نسخه نصبی (بدون نیاز به پایتون)
- ویندوز: `scripts/build_exe.bat` → خروجی `dist/DivarLead.exe`
- لینوکس: `scripts/build_exe.sh` → خروجی `dist/DivarLead`
- برای انتشار در GitHub: فایل خروجی را در بخش **Releases** پروژه ضمیمه کنید.

## 🧪 تست‌ها (۳۰ تست — همه پاس)
```bash
python -m unittest discover tests
```
شامل شبیه‌ساز کامل دیوار (`tests/mock_divar.py`) و جریان‌های:
لاگین OTP از وب، کلمات کلیدی چندتایی، قالب پیام، تنظیمات، شروع/توقف مانیتور،
گرفتن شماره، کپچا و ادامه بقیه اکانت‌ها، خروجی CSV، لاگ‌ها.

## 📚 مستندات
- [`docs/feasibility-research.md`](docs/feasibility-research.md) — امکان‌سنجی + لاگین + شماره تماس
- [`docs/challenges-and-countermeasures.md`](docs/challenges-and-countermeasures.md) — چالش‌ها/راهکارها + چند اکانت
- خط فرمان: `python -m marketing_divar --help` (login/collect/monitor/draft/export)

## 📁 ساختار
```
marketing_divar/         موتور (کلاینت دیوار، مانیتور، اکانت‌ها، دیتابیس، وب)
marketing_divar/web/     رابط گرافیکی (FastAPI + صفحه فارسی راست‌چین)
tests/                   ۳۰ تست + شبیه‌ساز دیوار
scripts/                 اجرا و ساخت نسخه نصبی
data/, logs/             داده‌ها و لاگ‌ها (به گیت نمی‌روند)
```

## 📥 دانلود و اجرای سریع روی ویندوز
1. از [بخش Releases](../../releases) → آخرین نسخه → **Source code (zip)** را دانلود و Extract کنید
2. یک‌بار برای همیشه: [پایتون 3.11](https://python.org/downloads) نصب کنید (تیک **Add to PATH** الزامی است)
3. روی **`شروع-دیوار-لید.bat`** دابل‌کلیک کنید → مرورگر باز می‌شود (`http://localhost:8642`)
4. اتصال واقعی به دیوار فقط با **IP ایران** برقرار می‌شود

نسخه تک‌فایل exe بدون نیاز به پایتون: روی ویندوز خود `scripts/build_exe.bat` را اجرا کنید → `dist/DivarLead.exe`

## 🌐 دو زبانه (v1.3.0)
دکمه 🌐 در نوار بالای برنامه: فارسی ⇄ English — کل رابط (منوها، فرم‌ها، دنباله رخدادها) جابه‌جا می‌شود و انتخاب در مرورگر ذخیره می‌ماند.

## 🖥️ نصب‌کننده گرافیکی جدید (v1.3.0)
دابل‌کلیک روی **Install-and-Run.bat** (یا شروع-دیوار-لید.bat):
- پنجره ویندوزی با **نوار پیشرفت واقعی** + لاگ زنده انگلیسی خوانا (مشکل نمایش فارسی در cmd حل شد)
- دکمه تغییر زبان داخل نصب‌کننده (فارسی/English)
- دانلود پایتون با درصد پیشرفت + **رفع SmartScreen** (Unblock-File) + نصب بی‌صدا با انتظار تا پایان
- نصب کتابخانه‌ها با نمایش خروجی + تلاش خودکار با mirror
- تست سلامت قبل از اجرا + اجرای برنامه و باز شدن مرورگر
- لاگ کامل نصب: `installer/install-log.txt`
- اگر خطایی رخ دهد، پنجره باز می‌ماند و دکمه «شروع نصب» برای تلاش دوباره فعال است

## v1.4.0 — ریشه‌یابی «همه‌چیز صفر» (۱۴۰۵/۰۶)
- **خودترمیمی پروکسی:** روی ویندوز اگر VPN/پروکسی سیستم فعال باشد و دیوار را قطع کند،
  برنامه خودش یک‌بار بدون پروکسی (اتصال مستقیم — درست برای IP ایران) تلاش می‌کند.
- **شفافیت کامل رخدادها:** شروع هر دور، خطای جستجو (+ راهنمای VPN/اینترنت)، شماره‌های
  پیداشده، کپچا/بلاک/سهمیه — همه در پنل «رخدادها» دیده می‌شوند؛ دیگر «بی‌صفر و بی‌خبر» نیست.
- پیام ۴۰۳ دیوار راهنمای صریح دارد: «VPN را خاموش کنید — دیوار فقط با IP ایران».
- ۴۰ تست (۲ بار): همه پاس؛ سناریوی کامل (لاگین + موارد فعلی + شروع) سرتاسری تأیید شد.

## v1.5.0 — چند-مسیری اتصال + بررسی اتصال کامل (۱۴۰۵/۰۶)
- **۴ مسیر اتصال پشت‌سرهم:** requests با پروکسی سیستم → requests مستقیم → httpx → urllib؛
  مسیر برنده به‌خاطر سپرده می‌شود (sticky). حداقل یکی در هر شرایطی جواب می‌دهد.
- **راه نجات جستجو:** اگر API جستجو خطا داد یا ۰ آگهی برگرداند، HTML خود سایت
  دیوار (divar.ir/s/…) خوانده و توکن+عنوان آگهی‌ها استخراج می‌شود.
- **دکمه «بررسی اتصال کامل» در تنظیمات:** DNS → اتصال → جستجو → آگهی واقعی
  (عنوان+متن+مکان کلمه کلیدی) → شماره بدون لاگین (آزمایش) → شماره با اکانت →
  دسترسی چت (بهترین‌حال) → وضعیت پروکسی؛ هر قدم ✓/✗ + زمان + جزئیات.
- **لاگ فوق‌دقیق:** هر درخواست HTTP با مسیر استفاده‌شده و زمان پاسخ، هر سرنخ
  جدید با مکان کلمه (عنوان/متن) در پنل رخدادها.
- ۴۶ تست (۲ بار): همه پاس.

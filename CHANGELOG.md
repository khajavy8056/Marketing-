# Changelog

## 1.6.0 — پایداری عملیاتی (۱۴۰۵/۰۶/۰۱ / 2026-08-23)

نسخه فعلی بازنویسی نشد؛ فقط باگ‌های واقعی و کمبودهای عملیاتی اصلاح شد.

### مشکلات نسخه قبل و ریشه آن‌ها

| مشکل | ریشه | اصلاح |
|---|---|---|
| فاصلهٔ شماره‌گیری عملاً اعمال نمی‌شد | `RateLimiter.wait("phone")` هیچ‌وقت صدا زده نمی‌شد | انتظار قبل از `get_phone` |
| مسیر HTML جستجو با کد عددی شهر (`/s/1`) ساخته می‌شد | اسلاگ شهر (`tehran`) لازم است | `city_slug` |
| حذف کلمه کلیدی از پنل کار نمی‌کرد | `act()` همیشه POST می‌فرستاد | پشتیبانی DELETE |
| هدر Authorization در مسیر httpx/urllib حذف می‌شد | `kw["headers"]` ادغام نمی‌شد | ادغام هدر در ترابری‌ها |
| همه نتایج جستجوی دیوار به‌عنوان سرنخ ذخیره می‌شد | تطبیق محلی عنوان/متن نبود | `matching.py` + نرمال‌سازی فارسی |
| Job رهاشده بعد از Restart در `processing` می‌ماند | وضعیت processing وجود نداشت | mark + reclaim |
| اعلان تلگرام ساخت‌یافته نبود | فقط یک رشته آزاد | اکانت/مشکل/عملیات/اقدام |
| `config.json` ممکن بود به گیت برود | در gitignore نبود | اضافه شد |

### قابلیت‌های اضافه‌شده (حداقلی)

- ذخیره توضیح، کلمات منطبق، زمان انتشار، وضعیت چت، شمارنده تلاش
- جدول `operations` برای ردگیری هر شماره‌گیری
- پیش‌نویس چت و ثبت وضعیت از پنل وب
- KPIهای داشبورد: کل / پیدا شده / بدون شماره / خطا
- خروجی CSV با ستون‌های کامل‌تر (سازگار با اکسل)

### تحقیق اتصال به دیوار (بدون حدس)

- **کنار (رسمی):** جستجو فیلترساختاری است، کلمه‌کلیدی آزاد ندارد؛ چت فقط با OAuth کاربر. بدون کلید تأییدشده قابل اتکا نیست.
- **API وب دیوار (`api.divar.ir/v8/web-search`):** همان مسیری است که سایت استفاده می‌کند؛ Discovery بدون لاگین ممکن است. شماره تماس نیاز به سشن دارد.
- **نتیجه:** مسیر فعلی Repository درست است و عوض نشد. فقط پایداری و تطبیق محلی تقویت شد.

### محدودیت‌های باقی‌مانده

- اتصال زنده به دیوار فقط از IP ایران
- ارسال خودکار چت پیاده نشده (عمدی — نیمه‌خودکار)
- SMS خارج از scope است
- API غیررسمی ممکن است بدون اطلاع عوض شود

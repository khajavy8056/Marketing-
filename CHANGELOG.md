# Changelog

## 3.3.0 — ورود برنامه، نوار اعتبار، شیپور/رینگ هم‌سطح دیوار (۱۴۰۵/۰۶/۰۷)

ویندوز. لینوکس سرور هنوز نیست.

- صفحهٔ ورود اختصاصی داخل خود برنامه (نه روی هاست): نام کاربری، رمز، به‌خاطر سپردن
- دفترچه همان `license/ok.csv` روی گیت با لینک خام؛ بعداً `DIVAR_LICENSE_URL` را عوض کنید
- ترتیب چک: اول نام کاربری در سطرها، بعد رمز، بعد زمان از هدر HTTP Date (نه ساعت ویندوز)
- بدون اینترنت ورود نیست. تب «اشتراک» نوار روزهای مانده را نشان می‌دهد
- تغییر رمز و پشتیبانی هنوز نیست
- پروفایل Chromium سه تب دیوار/شیپور/رینگ دست نخورده است
- جستجو و شکارچی روی هر پلتفرم فعال هم‌زمان؛ شماره از مسیر همان سایت؛ اگر شماره باشد پیامک، وگرنه چت
- چت بین اکانت‌ها می‌چرخد؛ متن قالب با متغیر و سلام/خداحافظی تصادفی فرق می‌کند


## 3.1.0 — شهر کامل، نگاشت دسته، شکارچی خودرو، مدل کنار برنامه (۱۴۰۵/۰۶/۰۷)

ویندوز. لینوکس سرور هنوز نیست.

- پلتفرم دیوار/شیپور/رینگ جدا خاموش و روشن می‌شود؛ اگر دیوار خاموش باشد جستجوی دیوار نمی‌رود
- دسته واحد کامل‌تر؛ اسلاگ شیپور و رینگ از روی انتخاب کاربر **خودکار نگاشت** می‌شود
- فهرست شهر کامل‌تر + انتخاب **چند شهر** در یک پایش
- شکارچی خودرو: شاسی ضربه، دوررنگ، تصادف، مدل و کارکرد — قیمت پایین این موارد شکار عالی نیست
- مدل محلی از اول نقش ثابت دارد (درک پاسخ، بررسی آگهی، خودرو، تصویر)؛ معامله نمی‌بندد
- دانلود مدل هنگام نصب: کش کنار فایل نصبی، نصب کنار پوشه برنامه (`nlu-model`)
- تحلیل پاسخ برای هر سه پلتفرم یکسان است (فارسی)
- تست اتصال مدل و مسیر سه‌پلتفرمه (۲۵۱ تست)

## 3.0.0 — چت خودکار، سه پلتفرم، هوش محلی (۱۴۰۵/۰۶/۰۷)

ویندوز. نسخه لینوکس سرور در این انتشار نیست.

- آگهی فقط‌چت: ارسال خودکار قالب با متغیر `{title}` و greeting/closing تا متن‌ها یکسان نباشند
- پاسخ چت به **همان آگهی** وصل می‌شود (`thread_id` / توکن در URL)؛ چت حذف‌شده کرش نمی‌کند
- پولینگ صندوق ورودی ملی‌پیامک (GetMessage / GetMessages) و تحلیل پاسخ
- مدل محلی Qwen 1.5B GGUF: اگر نباشد یک‌بار در پوشه پایدار کاربر دانلود می‌شود؛ داخل گیت نیست؛ API ابری چرخشی نداریم
- تب پنل «ربات هوشمند» وضعیت هوش، چت، صندوق و شکارچی را نشان می‌دهد
- یک پروفایل Chromium سه تب دیوار / شیپور / رینگ برای لاگین و کپچا باز می‌کند
- سوییچ مستقل دیوار، شیپور، رینگ + شکارچی قیمت + جداسازی معیوب / قیمت ساختگی / استعلام
- خروجی CSV ستون پلتفرم، شکارچی، نوع قیمت، معیوب، استعلام
- ادغام قابلیت‌های ۲.۳.۱: پترن ملی‌پیامک، GetDeliveries2، چرخش اکانت چت

## 2.3.1 — ساخت متن پترن در خود برنامه + کپی فرمت پنل (۱۴۰۵/۰۶/۰۶)

- **سازندهٔ متن پترن در برنامه:** در تنظیمات، کادر «متن پترن» اضافه شد؛ متن را با
  متغیرهای `{title}`، `{city}`، `{price}` و… می‌نویسید و برنامه همان‌جا نسخهٔ
  آمادهٔ پنل را نشان می‌دهد.
- **کپی با یک کلیک:** دکمهٔ «کپی متن پترن (فرمت پنل)» متن را به شکل `{0}`، `{1}`،
  `{2}` (فرمت رسمی ملی‌پیامک) تبدیل و کپی می‌کند — فقط Paste در پنل و تأیید می‌ماند.
- **بررسی خودکار قوانین:** برنامه زنده چک می‌کند که پترن متغیر داشته باشد، فقط
  متغیر نباشد و لینک/آدرس نداشته باشد (هر سه باعث رد پترن می‌شوند).
- **ترتیب متغیرها خودکار** از متن پترن پر می‌شود (`sms_pattern_args`).
- دکمهٔ «نمونهٔ آماده» برای شروع سریع + ذخیرهٔ متن پترن در تنظیمات (`sms_pattern_text`).
- راهنمای پترن (`docs/sms-pattern-guide.md`) با مراحل ساخت در برنامه به‌روز شد.

## 2.3.0 — ارسال پیامک با پترن (خط خدماتی) ملی‌پیامک (۱۴۰۵/۰۶/۰۶)

- **روش جدید «پترن»:** کنار روش «خط اختصاصی»، ارسال با `BaseServiceNumber` اضافه شد.
  پترن به شمارهٔ خط اختصاصی نیاز ندارد؛ فقط نام کاربری + رمز پنل + کد پترن (bodyId).
- **مزیت پترن:** چون متن از قبل در پنل تأیید شده، به شماره‌های لیست سیاه هم می‌رسد.
- **تنظیمات جدید:** انتخاب روش (خط اختصاصی / پترن)، کد پترن (`sms_pattern_bodyid`) و
  ترتیب متغیرها (`sms_pattern_args`). وقتی پترن فعال است، متنِ واقعیِ پیامک همان متنِ
  تأییدشده در پنل است و برنامه فقط مقادیر متغیرها را به ترتیب می‌فرستد.
- **راهنمای کامل پترن** (قوانین تأیید، نمونه متن، جدول خطاها) در `docs/sms-pattern-guide.md`.
- تست‌های جدید پترن (مجموع: ۲۲۵ تست پاس).

## 2.2.2 — چرخش اکانت چت + گزارش تحویل پیامک (نسخه نهایی) (۱۴۰۵/۰۶/۰۶)

- **چرخش اکانت در چت دیوار:** ارسال چت بین اکانت‌های فعال می‌چرخد (کم‌مصرف‌ترین
  اکانت اول) تا یک اکانت تنها مسدود نشود. مصرف هر اکانت بعد از هر ارسال بالا می‌رود
  و اکانتِ بعدی صف را برمی‌دارد.
- **ردپای چت:** برای هر ارسال، `chat_account` (کدام اکانت)، `chat_sent_at` (زمان) و
  ردیف `operations` (operation=chat) ثبت می‌شود — می‌توانید ببینید هر آگهی با کدام
  اکانت و چه زمانی پیام گرفته است.
- **گزارش تحویل پیامک (ملی‌پیامک):** RecId هر پیامک ذخیره می‌شود و با
  `GetDeliveries2` وضعیت تحویل (تحویل‌شده / ناموفق / در انتظار) خوانده می‌شود.
  دکمهٔ «بررسی وضعیت تحویل پیامک‌ها» در تنظیمات + نمایش وضعیت در تب سرنخ‌ها.
- شفاف‌سازی: تحویل چت دیوار رسید رسمی ندارد (فقط پذیرش ارسال ثبت می‌شود)؛
  تحویل پیامک از API رسمی ملی‌پیامک تأیید می‌شود.
- تست‌های جدید چرخش/تحویل (مجموع: ۲۲۰ تست پاس).

## 2.2.1 — شخصی‌سازی متغیری چت و پیامک (ضد اسپم) (۱۴۰۵/۰۶/۰۶)

- **متغیرهای غنی در قالب چت و پیامک:** علاوه بر `{title}` / `{subtitle}` / `{url}`،
  حالا `{city}` شهر، `{keyword}` کلمه/دسته، `{price}` قیمت (خوانا مثل «۲.۵ میلیون تومان»)
  و `{published_at}` زمان انتشار هم پر می‌شوند.
- **گردونهٔ سلام/خداحافظی:** متغیرهای `{greeting}` و `{closing}` هر بار یک متن تصادفی
  از چند گزینه می‌گیرند تا پیام‌ها (چت و پیامک) یکسان نباشند — الگوی تکراری که
  ساده‌ترین سیگنال اسپم است حذف می‌شود.
- قالب پیش‌فرض چت به `{greeting}` / `{closing}` مهاجرت کرد.
- قالب‌های دارای متغیر ناشناخته دیگر کرش نمی‌کنند (خام برمی‌گردند).
- تست‌های جدید تنوع متن و پر شدن متغیرها (مجموع: ۲۱۷ تست پاس).

## 2.2.0 — ارسال خودکار چت دیوار + راهنمای «؟» پیامک/چت (۱۴۰۵/۰۶/۰۶)

- **ارسال خودکار چت دیوار** برای سرنخ‌های «فقط چت»: وقتی شماره‌ای نباشد و فقط چت
  فعال باشد، متن قالب چت با اکانت لاگین‌شده همان لحظه به صاحب آگهی می‌رود.
  - کلید خاموش/روشن `chat_auto_on_new` (پیش‌فرض **خاموش**)، سقف روزانه
    `chat_auto_daily_limit` (پیش‌فرض ۲۰) و تأخیر `chat_auto_delay_sec` (پیش‌فرض ۶۰).
  - هر شکست خودکار به `chat_status='requires_operator'` برمی‌گردد و جریان
    نیمه‌خودکار (اپراتور) ادامه می‌دهد — هرگز «ارسال موفق» جعلی ثبت نمی‌شود.
  - ⚠️ مطابق مستندات، این مسیر پرریسک است؛ با سقف/تأخیر پایین نگه داشته می‌شود.
- **راهنمای «؟» (تولتیپ)** روی فیلدهای ملی‌پیامک و قالب چت: توضیح دقیق هر فیلد
  (نام کاربری، رمز/کلید API، خط ارسال، سقف) هنگام بردن موس.
- **تست ارسال چت**: دکمهٔ «تست ارسال چت» اولین سرنخ فقط‌چت را با قالب ذخیره‌شده
  ارسال می‌کند و متن پیش‌نمایش را نشان می‌دهد.
- شمارندهٔ روزانهٔ «چت» به سهمیه اضافه شد (`chats`) و در داشبورد نمایش داده می‌شود.
- شمارندهٔ پیامک/چت امروز در داشبورد و سقف‌ها دقیق‌تر شد.

## 2.1.25 — ارسال واقعی بله و روبیکا (۱۴۰۵/۰۶/۰۴)

دکمهٔ بررسی اتصال دیگر با HTTP ۲۰۰ خالی «ارسال موفق» نشان نمی‌دهد.
بله فقط وقتی `ok===true` باشد موفق است؛ روبیکا فقط با `status=OK`.
اگر صفحه‌کلید رد شود همان پیام بدون دکمه دوباره می‌رود.
شناسه گفتگوی عددی بله به‌صورت عدد فرستاده می‌شود.

## 2.1.24 — نصب مثل ۲.۱.۲۲ + دکمه خروج + شورتکات (۱۴۰۵/۰۶/۰۴)

نصب دوباره همان `Install-and-Run.bat` است (بدون کپی به پوشهٔ جدا).
اگر پایتون روی ویندوز نباشد خودش از python.org دانلود و نصب می‌شود.
بعد از نصب میانبر دسکتاپ ساخته می‌شود و برنامه اجرا می‌گردد.
دکمهٔ «خروج» در پنل کل برنامه را می‌بندد. هر بار باز شدن، همان لودینگ می‌آید.

## 2.1.23 — نصب‌کننده واحد + اسپلش ۴ دقیقه + سقف ۶۰ (۱۴۰۵/۰۶/۰۴)

یک نصب‌کننده گرافیکی: پوشهٔ نصب را خودتان انتخاب می‌کنید، نوار پیشرفت پر
می‌شود، فایل‌ها و پوشه‌های برنامه آنجا ساخته می‌شوند. ساخت Setup تک‌فایل
کل پروژه را داخل همان exe می‌گذارد. کنسول اجرا کمینه/مخفی است.
صفحهٔ اتصال هر بار حدود ۴ دقیقه است (فقط ظاهر). پیش‌فرض تنظیمات:
سقف روزانه هر اکانت ۶۰، فاصله شماره‌گیری ۴۵ ثانیه، دوره اسکن ۳۰۰ ثانیه.

## 2.1.22 — صفحه لودینگ نمایشی قبل از پنل (۱۴۰۵/۰۶/۰۴)

هر بار باز شدن پنل حدود دو دقیقه صفحهٔ «در حال اتصال به سرورها» با نوار پیشرفت
نشان می‌دهد. فقط ظاهر است؛ اسکن و API عوض نشده. بعد از ورود، پایین صفحه
وضعیت «متصل به سرور» و پینگ نمایشی می‌آید.

## 2.1.21 — صفحه لاگین دیوار + اطلاع‌رسانی رسمی (۱۴۰۵/۰۶/۰۴)

پروفایل Chromium مستقیم `divar.ir/user` را باز می‌کند.
تلگرام / بله / روبیکا هر کدام تیک «استفاده شود» و دکمهٔ بررسی اتصال دارند؛
دکمه پیام «ارتباط برقرار شد» می‌فرستد. دکمه‌های پایین ربات: گزارش امروز،
سرنخ امروز، همه شماره‌ها، آلارم مهم، اکسل.

## 2.1.20 — ذخیره از کوکی دیسک + کپچا همان اکانت (۱۴۰۵/۰۶/۰۴)

ذخیره پروفایل اگر CDP کوکی ندهد، نام کوکی‌های SQLite همان پوشهٔ Chromium
را می‌خواند (بدون تزریق). اگر لاگین روی دیسک باشد ذخیره قبول می‌شود.
«کپچا حل شد» همان اکانت را آزاد می‌کند و فوراً یک شماره از صف می‌گیرد.

## 2.1.19 — پروفایل اکانت قبل از باز شدن دیوار (۱۴۰۵/۰۶/۰۴)

«باز کردن دیوار» دیگر تب خالی روی Chromium پنل باز نمی‌کند.
اول پوشهٔ پروفایل همان اکانت ساخته می‌شود، بعد همان `user-data-dir` با
نام اکانت باز می‌شود (بدون `--new-window`). ذخیره از همان پنجرهٔ باز
کوکی می‌خواند. نام فارسی روی دیسک به مسیر ASCII می‌رود تا Chromium ویندوز
پروفایل را دور نیندازد.

## 2.1.18 — ادامهٔ دانلود Chromium بعد از قطع کوتاه (۱۴۰۵/۰۶/۰۴)

قطع لحظه‌ای اینترنت دیگر منبع را عوض نمی‌کند و از صفر شروع نمی‌کند.
دانلود با Range روی همان URL ادامه می‌یابد (فایل `.part` در پوشهٔ نصب می‌ماند).
زیپ فقط داخل `app-chromium` ذخیره می‌شود (نه Downloads). اگر همان زیپ
معتبر از قبل آنجا باشد، بدون دانلود دوباره extract و ثبت می‌شود و بعد از
نصب هم زیپ پاک نمی‌شود تا تلاش بعدی دانلود را رد کند.

## 2.1.17 — Chromium واقعی، نه Chrome/Edge؛ نصب ثبت‌شده؛ پروفایل پایدار (۱۴۰۵/۰۶/۰۴)

- منبع Chrome for Testing حذف شد (آن محصول Google Chrome است، نه Chromium)
- Installed فقط بعد از extract + تشخیص محصول Chromium + `INSTALLED.json`
- پنل دیگر با مرورگر پیش‌فرض (Edge) باز نمی‌شود؛ فقط Chromium اختصاصی
- پنجرهٔ لاگین با خودِ `chrome.exe` اختصاصی باز می‌شود (نه Playwright thread)
- ذخیرهٔ پروفایل دیگر پنجرهٔ موقت باز/بسته نمی‌کند؛ فقط همان پنجرهٔ باز
- خطای هر مرحله در لاگ و پیام پنل می‌آید

## 2.1.16 — نوار مستقل Chromium + چندمنبع بدون گیر (۱۴۰۵/۰۶/۰۴)

نصب دیگر روی دانلود Chromium گیر نمی‌کند. هر منبع اول با timeout کوتاه
پروب می‌شود؛ میزبان مرده چند ثانیه رد می‌شود. فایل ناقص پاک می‌شود؛
SHA256 (اگر باشد) و CRC زیپ قبل از extract چک می‌شود.
نوار پیشرفت Chromium از نوار نصب برنامه جداست (شروع / درصد / بایت / سرعت / Completed).
برنامه همیشه همان `chrome.exe` داخل `app-chromium` را باز می‌کند، حتی اگر
Chrome/Edge سیستم نصب باشد. فورک سورس Chromium لازم نیست.

## 2.1.15 — دانلود Chromium از GitHub با درصد پیشرفت (۱۴۰۵/۰۶/۰۴)

نصب دیگر روی «App Chromium» گیر نمی‌کند.
به‌جای CDN فیلترشدهٔ Playwright، **ungoogled-chromium** متن‌باز از GitHub
(با آینه) با نوار درصد دانلود می‌شود و در
`%LOCALAPPDATA%\\DivarMarketing\\app-chromium` می‌ماند.
پنجرهٔ دیوار فقط همین مرورگر اختصاصی است، نه Chrome/Edge کاربر.

## 2.1.14 — Chromium اختصاصی برنامه، نه پوشهٔ موقت exe (۱۴۰۵/۰۶/۰۴)

خطای `Executable doesn't exist ... Temp\\_MEI...\\chrome.exe` از exe یک‌فایلی بود:
Playwright مرورگر را داخل پوشهٔ موقت PyInstaller می‌جست که پاک می‌شود.

- Chromium همیشه در `%LOCALAPPDATA%\\DivarMarketing\\app-chromium` نصب می‌شود (جدا از Chrome/Edge کاربر)
- نصب‌کننده (Setup.exe و Install-and-Run) همین نسخه را از چند آینه دانلود می‌کند
- پروفایل لاگین هر اکانت جدا می‌ماند؛ بعد از «ذخیره پروفایل» پنجره بسته می‌شود
- برنامه فقط همان Chromium اختصاصی را باز می‌کند، حتی اگر مرورگر پیش‌فرض چیز دیگری باشد

## 2.1.13 — پروفایل پایدار Chromium برای هر اکانت (۱۴۰۵/۰۶/۰۴)

سشن سایت دیگر تزریق نمی‌شود. هر اکانت پوشهٔ `chromium/` جدا دارد.
افزودن پروفایل → Chromium روی `divar.ir/s/tehran` → لاگین شما → ذخیره پروفایل.
دفعهٔ بعد همان پروفایل باز می‌شود و حساب لاگین‌شده است.
نصب‌کننده Chromium را با `playwright install chromium` می‌گیرد.

## 2.1.12 — لاگین دوگامه: توکن API + سشن سایت از مرورگر (۱۴۰۵/۰۶/۰۴)

- OTP دوباره همان مسیر قدیمی v5 است (کد پیامک دیگر با consume سایت ۴۰۰ نمی‌شود).
- بعد از لاگین، اکانت در لیست می‌آید؛ دکمهٔ «جمع‌آوری سشن سایت» صفحهٔ لاگین دیوار را باز می‌کند.
- کاربر همان‌جا وارد می‌شود؛ کوکی و localStorage واقعی ذخیره می‌شود؛ صفحه چک و بسته می‌شود.
- اگر صفحهٔ لاگین‌شده تأیید شود رتبهٔ اکانت سبز («تأییدشده») می‌شود. پازل فقط بعد از آن.

## 2.1.11 — صفحهٔ پازل با سشن کامل همان اکانت (۱۴۰۵/۰۶/۰۳)

- اگر consume با deviceId خطا بدهد (نه کد غلط)، مسیرهای جایگزین همان کد امتحان می‌شوند.
- کوکی‌های حالت header (`st-access-token` / `front-token`) هم قبل از Navigate تزریق می‌شوند.
- مقدار درصد-کدشدهٔ Set-Cookie باز می‌شود تا JWT خراب به مرورگر نرود.

## 2.1.10 — لاگین کامل برای صفحهٔ پازل (۱۴۰۵/۰۶/۰۳)

- پاسخ consume اگر SuperTokens را در JSON بدهد (access/refresh/front) ذخیره می‌شود.
- توکن API (v5) و کوکی سایت جدا می‌مانند؛ کوکی واقعی با JWT عوض نمی‌شود.
- اگر deviceId هست فقط یک بار consume می‌شود (کد پیامک نسوزد).
- قبل از باز شدن پازل: کوکی + localStorage (token و sFrontToken و sAccessToken).

## 2.1.9 — سشن سایت بعد از کد پیامک واقعاً گرفته می‌شود (۱۴۰۵/۰۶/۰۳)

- درخواست OTP فیلدهای SuperTokens (`deviceId` / `preAuthSessionId`) را نگه می‌دارد.
- تأیید کد همان‌ها را با `userInputCode` و هدر `rid: passwordless` مصرف می‌کند.
- اگر فقط توکن API بیاید، از همان JWT کوکی سایت ساخته می‌شود تا لاگین رد نشود.
- حساب قدیمیِ فقط-JWT هنوز در پنل دیده نمی‌شود.

## 2.1.8 — فقط اکانت با سشن کامل سایت (۱۴۰۵/۰۶/۰۳)

- حساب قدیمی که فقط JWT دارد در پنل نشان داده نمی‌شود و پازل برایش باز نمی‌شود.
- لاگین OTP اول مسیر سایت (v8/SuperTokens) را می‌گیرد؛ بعد توکن API.
- دکمهٔ پازل فقط وقتی سشن سایت کامل است کار می‌کند.

## 2.1.7 — لاگین کامل دیوار داخل پازل (۱۴۰۵/۰۶/۰۳)

- در تأیید OTP همهٔ سشن دیوار ذخیره می‌شود: token + کوکی SuperTokens + هدرهای st-* / front-token.
- قبل از باز شدن صفحه، کوکی روی divar.ir و api.divar.ir و localStorage تزریق می‌شود.
- بعد از حل دستی، دکمهٔ «کپچا حل شد» دوباره به دیوار می‌زند (حل خودکار اضافه نشد).

## 2.1.4 — خطای «پاسخ خالی از CDP» و پنجرهٔ پشتیبان (۱۴۰۵/۰۶/۰۳)

- پورت دیباگ را خود Edge می‌نویسد؛ قفل پروفایل قبلی پاک می‌شود.
- اگر تصویر داخل پنل نیامد، همان اکانت در پنجرهٔ Edge باز می‌شود تا پازل حل شود.
- حل خودکار پازل دیوار اضافه نشد (پازل ضدربات است؛ اپراتور حل می‌کند).

## 2.1.3 — پازل روی همان آگهی، بدون پروفایل دورریختنی (۱۴۰۵/۰۶/۰۳)

دیوار از API تصویر پازل نمی‌دهد؛ پروفایل موقت هم جواب را به برنامه برنمی‌گرداند.
حالا همان پروفایل پایدار هر اکانت باز می‌شود، صفحهٔ آگهی مسدود می‌آید،
و بعد از حل کوکی‌ها به `session.json` برمی‌گردند. لاگین پاک نمی‌شود.

## 2.1.2 — پازل فقط با دکمهٔ همان اکانت (۱۴۰۵/۰۶/۰۳)

- با باز شدن برنامه هیچ پازلی خودکار بالا نمی‌آید.
- فقط دکمهٔ کنار همان حساب سشن/کوکی را می‌آورد؛ قبلی بسته و پاک می‌شود.
- تصویر پازل داخل همان صفحهٔ پنل است.

## 2.1.1 — پازل: CDP بدون پروکسی و پروفایل جدا (۱۴۰۵/۰۶/۰۳)

- خطای زنده `urlopen timed out`: درخواست دیباگ مرورگر دیگر از پروکسی ویندوز رد نمی‌شود.
- پروفایل جدا برای پازل تا Edge باز قبلی پورت دیباگ را قورت ندهد.
- اتصال به تب صفحه (نه فقط browser target) تا کلیک/تصویر کار کند.

## 2.1.0 — پازل داخل همان پاپ‌آپ + بله/روبیکا + صدای اعلان (۱۴۰۵/۰۶/۰۲)

- پازل دیوار همان اکانت داخل `#cap-dlg` با تصویر زنده (نه iframe، نه پنجرهٔ جدا).
- اتصال رسمی بله (`tapi.bale.ai`) و روبیکا (`botapi.rubika.ir/v3`) با راهنمای توکن.
- اعلان داخل پنل با سه صدا: ویژه، شماره جدید، اکانت نیاز به واکنش.

## 2.0.4 — پازل همان اکانت در پنجرهٔ لاگین‌شده (۱۴۰۵/۰۶/۰۲)

- iframe دیوار حذف شد (سایت X-Frame می‌دهد و «refused to connect» بود)
- تب معمولی divar.ir دیگر پیشنهاد نمی‌شود — آن تب با اکانت برنامه لاگین نیست
- دکمهٔ «باز کردن پازل همین اکانت» Edge/Chrome را با کوکی/توکن همان `session.json` باز می‌کند
- اگر بدنهٔ ۴۰۳ تصویر داشته باشد در پاپ‌آپ نشان داده می‌شود؛ وگرنه فقط همان پنجره
- چک دوره‌ای قبلی سر جایش است: اگر پازل روی گوشی حل شده باشد اکانت خودکار آزاد می‌شود

## 2.0.3 — آزادسازی واقعی با همان اکانت دیوار (۱۴۰۵/۰۶/۰۲)

- دکمهٔ آزادسازی اول با همان سشن مسدود به دیوار می‌زند
- اگر پازل روی گوشی حل شده باشد اکانت خودکار باز می‌شود (چک هر ۲ دقیقه، بعد هر ۱۲ دقیقه)
- پاپ‌آپ پنل صفحهٔ دیوار را نشان می‌دهد + دکمهٔ «الان با همین اکانت بزن»
- پازل دیوار را اینجا حل نمی‌کنیم؛ اگر iframe خالی بود دیوار را روی گوشی باز کنید

## 2.0.2 — صف شماره و تلگرام فیلترشده (۱۴۰۵/۰۶/۰۲)

- پاسخ خالی/خطای تماس دیگر «فقط چت» نیست؛ آگهی در صف شماره‌گیری می‌ماند
- فقط وقتی دیوار صریحاً مخفی بودن را بگوید به لیست چت می‌رود
- ۴۰۳/۴۲۹ تماس همیشه پاپ‌آپ آزادسازی می‌آورد (حتی بدون کلمهٔ کپچا)
- بعد از آزادسازی، صف همان لحظه ادامه می‌یابد (نه بعد از کل فاصلهٔ اسکن)
- تلگرام: پروکسی و آدرس Bot API سفارشی؛ بدون ربات پنل کامل کار می‌کند
- دکمهٔ برگشت آگهی‌های اشتباهِ فقط‌چت به صف شماره

## 2.0.1 — سقف هوشمند ۱۲۹ + پاپ‌آپ کپچا داخل پنل (۱۴۰۵/۰۶/۰۲)

- عدد ۶۰ سقف نرم‌افزاری خود برنامه بود، نه دیوار؛ پیش‌فرض هر اکانت ۱۲۹
- نصب‌های قدیمی که ۶۰ ذخیره کرده بودند یک‌بار به ۱۲۹ ارتقا می‌یابند
- بعد از ۱۲۹ اگر دیوار کپچا/۴۲۹ ندهد با فاصلهٔ بیشتر ادامه می‌دهد
- وقتی دیوار کپچا بخواهد پاپ‌آپ داخل همین پنل می‌آید (جمع ساده برای تأیید حضور)
- کپچای دیوار را نمی‌شود اینجا جایگزین کرد؛ دکمهٔ باز کردن دیوار برای چالش واقعی

## 2.0.0 — شهر و زیردسته دیوار + بازه قیمت + هشدار ویژه (۱۴۰۵/۰۶/۰۲)

- شهر مثل دسته: دراپ‌داون شهرهای دیوار (`/s/{شهر}/…`)
- دسته‌ها با دستهٔ اصلی و زیر‌دسته (موبایل زیر موبایل‌وتبلت، خودرو زیر وسایل نقلیه، …)
- بازه قیمت روی هر پایش (میلیون تومان)؛ فقط آگهی داخل بازه سرنخ می‌شود
- تیک هشدار ویژه: تلگرام فوری + صف شماره؛ در تنظیمات قابل خاموش کردن
- نصب همان تک‌فایل Setup ویندوز است (`ساخت-نصب-استاندارد.bat`)

## 1.9.2 — دسته = لیست خود دیوار، نه عنوان آگهی (۱۴۰۵/۰۶/۰۲)

- فقط‌دسته یعنی مرور `/s/{شهر}/{دسته}` مرتب‌شده با تاریخ؛ عنوان (S25 اولترا و …) فیلتر نمی‌شود
- فلگ `browse` ذخیره می‌شود تا با عوض شدن برچسب دسته خراب نشود
- «موارد فعلی هم گرفته شوند» چند صفحه از همان دسته را برمی‌دارد
- اسلاگ دقیق «موبایل» = `mobile-phones`

## 1.9.1 — دستهٔ بدون کلمه واقعاً آگهی جدید می‌گیرد (۱۴۰۵/۰۶/۰۲)

- باگ: مانیتور `match_all` را به تطبیق آگهی نمی‌داد؛ عنوان دسته (مثلاً «موبایل و تبلت») مثل کلمهٔ اجباری فیلتر می‌شد و هیچ سرنخ جدیدی از لیست دسته ذخیره نمی‌شد
- حالا فقط‌دسته = همهٔ آگهی‌های همان صفحه (آگهی‌های جدید بالای لیست دیوار)

## 1.9.0 — Divar Marketing + LAN + one-file Setup (۱۴۰۵/۰۶/۰۲)

- Product name: **Divar Marketing** / پنل: **مارکتینگ دیوار**
- Console, installer, and Setup window are English only
- One-file Windows installer: run `ساخت-نصب-استاندارد.bat` on Windows → `DivarMarketing-Setup.exe` (no other files)
- Desktop + Start Menu shortcut with the app logo
- Listens on `0.0.0.0:8642` so a phone on the same Wi-Fi can open `http://PC-IP:8642`
- Settings folder: `%LOCALAPPDATA%\DivarMarketing` (migrates KhajavyLead once)

## 1.8.0 — دسته‌بندی دیوار + ساخت exe استاندارد روی ویندوز شما (۱۴۰۵/۰۶/۰۲)

- تب کلمات: انتخاب دستهٔ واقعی دیوار (موبایل، خودرو سواری، رایانه، املاک، …)
- می‌توان فقط دسته، فقط کلمه، یا هر دو را با هم پایش کرد
- مسیر زنده: `divar.ir/s/{شهر}/{دسته}` بدون لاگین
- این محیط لینوکس exe ویندوز نمی‌سازد؛ فایل `ساخت-نصب-استاندارد.bat` را روی ویندوز باز کنید تا `DivarLead.exe` ساخته شود

## 1.7.1 — نصب دیگر پنجرهٔ سیاه را بی‌صدا نمی‌بندد (۱۴۰۵/۰۶/۰۲)

دابل‌کلیک روی `Install-and-Run.bat` پنجره را باز و فوراً نمی‌بندد.

- `installer.ps1` با UTF-8 BOM و CRLF تا PowerShell ویندوز آن را بخواند
- اگر پنجرهٔ گرافیکی باز نشد، نصب کنسولی (`install-console.ps1`) در همان CMD ادامه می‌دهد
- اگر فایل‌ها Extract نشده باشند پیام واضح می‌آید و پنجره باز می‌ماند
- `شروع-دیوار-لید.bat` دیگر با venv خراب برنامه را استارت و خارج نمی‌شود
- اگر خود برنامه کرش کند، پنجره سیاه بسته نمی‌شود تا متن خطا دیده شود

## 1.7.0 — نسخه کامل تجاری خواجوی لید (۱۴۰۵/۰۶/۰۱)

تنظیمات بعد از بستن برنامه و نصب دوباره حفظ می‌شوند:
تلگرام، ملی‌پیامک، کلمات کلیدی، قالب‌ها، اکانت‌ها و سرنخ‌ها در پوشهٔ ثابت
کاربر (`%LOCALAPPDATA%\\KhajavyLead` در ویندوز).

- نصب گرافیکی با نوار پیشرفت + میانبر دسکتاپ «خواجوی لید»
- دابل‌کلیک `شروع-دیوار-لید.bat` اگر نصب شده باشد خود برنامه را باز می‌کند
- نصب‌کننده دادهٔ کاربر را پاک نمی‌کند
- پیامک خودکار ملی‌پیامک، ربات تلگرام با دکمه و اکسل زمانی، کپچای دستی، تم

## 1.6.6 — ربات تلگرام: اعلان سرنخ جدید + دکمه‌ها + اکسل با ساعت استخراج (۱۴۰۵/۰۶/۰۱)

- به محض پیدا شدن شماره، پیام تلگرام با شمارندهٔ امروز و تاریخ‌ساعت استخراج
- اگر پیامک خودکار برود، همان لحظه هم در ربات اعلام می‌شود
- دکمه‌های پایین ربات: گزارش امروز، سرنخ‌های امروز، خروجی اکسل، راهنما
- فایل اکسل شامل تاریخ‌ساعت کشف، استخراج شماره، انتشار آگهی و ارسال پیامک

## 1.6.5 — پیامک خودکار همان لحظهٔ پیدا شدن شماره (۱۴۰۵/۰۶/۰۱)

- تیک «ارسال خودکار پیامک به محض پیدا شدن شماره» روی داشبورد، تب پیام‌ها و تنظیمات
- روشن شدن تیک همان لحظه روی مانیتور اعمال می‌شود (ری‌استارت لازم نیست)
- متن قالب تب پیام‌ها از اکانت ملی‌پیامک به همان شماره می‌رود
- دکمهٔ ارسال دستی روی هر سرنخ شماره‌دار
- باگ سقف پیامک که ارسال موفق را خطا نشان می‌داد برطرف شد

## 1.6.4 — بسته تجاری خواجوی لید (۱۴۰۵/۰۶/۰۱)

- نام محصول **خواجوی لید** + پاورقی «طراحی و توسعه توسط خواجوی»
- تم تیره/روشن/خودکار (ساعت و سیستم) با دکمه خورشید/ماه
- توضیح سقف روزانه IP: افزایش عدد محدودیت دیوار را دور نمی‌زند؛ ظرفیت بیشتر = IP/دستگاه جدا
- کارت کپچای انسانی روی اکانت: باز کردن divar.ir، حل دستی، آزادسازی (بدون حل‌کننده)
- ربات تلگرام ادمین: `/status` `/today` `/leads` `/release`
- اتصال رسمی ملی‌پیامک (username+password+خط) — ارسال خودکار پیش‌فرض خاموش + سقف روزانه
- چت همچنان نیمه‌خودکار است

## 1.6.3 — جستجو دیگر HTML سالم را بلاک حساب نمی‌کند (۱۴۰۵/۰۶/۰۱)

لاگ زنده کاربر: DNS و HTTPS سالم بودند؛ جستجو با
`DivarBlockedError: HTML جستجو هم HTTP 200 داد` می‌ترکید.
ریشه: صفحهٔ عادی دیوار پر از JS با کلمهٔ `challenge`/`captcha` است و
تشخیص کپچا غلط بود. بعد از آن جزئیات و شماره اصلاً اجرا نمی‌شدند.

- HTML ۲۰۰ دیگر کپچا نیست؛ فقط ۴۰۳/۴۲۹ HTML بلاک است
- ۴۰۳ روی API جستجو دیگر کل Discovery را نمی‌کشد — HTML امتحان می‌شود
- پارس توکن تکی `/v/TOKEN` و `"token":"..."` داخل HTML
- KeyError توکن در بررسی اتصال بعد از شکست جستجو برطرف شد

## 1.6.2 — لاگین/شماره کامل + نصب ویندوز با نوار پیشرفت (۱۴۰۵/۰۶/۰۱)

- دکمهٔ چت سرنخ در پنل واقعاً کار می‌کند (پیش‌نویس + کپی + باز کردن آگهی + ثبت ارسال)
- «بررسی اتصال کامل» همان زنجیرهٔ زندهٔ جستجو و `contact_uuid` تو‌در‌تو را می‌زند
- تست OTP وب → گرفتن شماره با همان سشن (روی شبیه‌ساز)
- نصب‌کننده ویندوز: نوار پیشرفت، دانلود پایتون با درصد، Unblock-File، محیط مجازی `.venv`، تست سلامت، میانبر دسکتاپ، اجرا و باز شدن مرورگر

لاگین واقعی و شمارهٔ زنده را خود کاربر روی IP ایران تست می‌کند.

## 1.6.1 — جستجو بعد از مرگ web-search (۱۴۰۵/۰۶/۰۱ / 2026-08-23)

روی پاسخ زنده دیوار (نه mock) تأیید شد:

| مسیر | نتیجه زنده |
|---|---|
| `GET /v8/web-search/*` | `BLOCKING_VIEW` / «نیاز به بروزرسانی» / `last_post_date: -1` |
| HTML عمومی `divar.ir/s/{شهر}?q=` | آگهی واقعی (تهران و iran، کلمهٔ تدریس) |
| `GET /v8/posts-v2/web/{token}` | عنوان، توضیح، قیمت؛ `contact_uuid` تو‌در‌تو |
| `GET /v8/posts/{token}` | RBAC denied |

اصلاح‌ها (بازنویسی نشد):

- تشخیص `BLOCKING_VIEW` تا جستجو روی API مرده گیر نکند
- استخراج `contact_uuid` از مسیر زنده `contact.action_log.server_side_info.info`
- پارس HTML برای URL کامل `https://divar.ir/v/{slug}/{token}`
- مسیر سوم: `POST /v8/search/{شهر}` اگر GET خالی/مسدود باشد
- HTML غیر ۲۰۰ به‌جز ۴۰۳/۴۲۹ دیگر کل مانیتور را «بلاک» حساب نمی‌کند

شماره و چت زنده از این محیط تست نشد (TLS خروجی sandbox قطع است). Discovery همچنان بدون لاگین است.

## 1.6.0 — پایداری عملیاتی (۱۴۰۵/۰۶/۰۱ / 2026-08-23)

نسخه فعلی بازنویسی نشد؛ فقط باگ‌های واقعی و کمبودهای عملیاتی اصلاح شد.

### مشکلات نسخه قبل و ریشه آن‌ها

| مشکل | ریشه | اصلاح |
|---|---|---|
| فاصلهٔ شماره‌گیری عملاً اعمال نمی‌شد | `RateLimiter.wait("phone")` هیچ‌وقت صدا زده نمی‌شد | انتظار قبل از `get_phone` |
| مسیر HTML جستجو با کد عددی شهر (`/s/1`) ساخته می‌شد | اسلاگ شهر (`tehran`) لازم است | `city_slug` |
| حذف کلمه کلیدی از پنل کار نمی‌کرد | `act()` همیشه POST می‌فرستاد | پشتیبانی DELETE |
| هدر Authorization در مسیر httpx/urllib حذف می‌شد | `kw["headers"]` ادغام نمی‌شد | ادغام هدر در ترابری‌ها |
| همه نتایج جستجوی دیوار به‌عنوان سرنخ ذخیره می‌شد | تطبیق محلی عنوان/متن نبود | `matching.py` + نرمال‌سازی فارسی |
| Job رهاشده بعد از Restart در `processing` می‌ماند | وضعیت processing وجود نداشت | mark + reclaim |
| اعلان تلگرام ساخت‌یافته نبود | فقط یک رشته آزاد | اکانت/مشکل/عملیات/اقدام |
| `config.json` ممکن بود به گیت برود | در gitignore نبود | اضافه شد |

### قابلیت‌های اضافه‌شده (حداقلی)

- ذخیره توضیح، کلمات منطبق، زمان انتشار، وضعیت چت، شمارنده تلاش
- جدول `operations` برای ردگیری هر شماره‌گیری
- پیش‌نویس چت و ثبت وضعیت از پنل وب
- KPIهای داشبورد: کل / پیدا شده / بدون شماره / خطا
- خروجی CSV با ستون‌های کامل‌تر (سازگار با اکسل)

### تحقیق اتصال به دیوار (بدون حدس)

- **کنار (رسمی):** جستجو فیلترساختاری است، کلمه‌کلیدی آزاد ندارد؛ چت فقط با OAuth کاربر. بدون کلید تأییدشده قابل اتکا نیست.
- **API وب دیوار (`api.divar.ir/v8/web-search`):** همان مسیری است که سایت استفاده می‌کند؛ Discovery بدون لاگین ممکن است. شماره تماس نیاز به سشن دارد.
- **نتیجه:** مسیر فعلی Repository درست است و عوض نشد. فقط پایداری و تطبیق محلی تقویت شد.

### محدودیت‌های باقی‌مانده

- اتصال زنده به دیوار فقط از IP ایران
- ارسال خودکار چت پیاده نشده (عمدی — نیمه‌خودکار)
- SMS خارج از scope است
- API غیررسمی ممکن است بدون اطلاع عوض شود
